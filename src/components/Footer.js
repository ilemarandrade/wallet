import React from "react";
import { Box, Typography } from "@material-ui/core";

const Footer = () => (
  <Box sx={{ minHeight: 10, color: "white" }}>
    <Typography align="center">
      Creado por el web developer Ilemar Andrade
    </Typography>
  </Box>
);

export default Footer;
